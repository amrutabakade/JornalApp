$kind: http-request
name: Get All Users
method: GET
url: '{{baseUrl}}/user'
description: 'Retrieve a list of all users.'
auth:
  type: basic
  credentials:
    - key: username
      value: '{{username}}'
    - key: password
      value: '{{password}}'
